"""Phase 5 — Streamlit UI. STUB (thin layer; no modelling logic here).

The DS-facing demo. Planned tabs:
  - Strategy Lab       : pick a race + situation, see the recommended strategy
                         and the outcome distribution.
  - Telemetry Explorer : the Phase 1 EDA plots — fuel-corrected pace vs tyre age
                         by compound — made interactive.
  - Outcome Predictor  : the optional standalone podium predictor (Phase A).

Like the API, this calls into the package; it never models anything itself.
Run (Phase 5):  streamlit run app/streamlit_app.py
"""

# import streamlit as st  # enabled in Phase 5 (pip install .[app])
#
# st.title("F1 Strategy Engine")
# st.caption("Not 'who will win' — 'what should the team do'.")
