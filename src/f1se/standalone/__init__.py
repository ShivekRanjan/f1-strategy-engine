"""Phase A — standalone outcome predictors (podium + championship).

Genuinely separate from the strategy engine: results-only (no telemetry), its
own data, its own models. Kept here as the optional warm-up tab. Same rigour as
the main project — forward-in-time validation, beat a dumb baseline.
"""
