# For hosts that install the API from requirements.txt (e.g. Render).
# Installs this package with its [app] extra (FastAPI + uvicorn + base).
# Local dev should prefer:  pip install -e ".[app,dev]"
# The UI is the separate React app in frontend/ (its own npm install).
.[app]
